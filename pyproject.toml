[tool.taskipy.tasks]
dev = "uvicorn main:app --reload"
start = "uvicorn main:app"
install = "pip install -r requirements.txt"
freeze = "pip freeze > requirements.txt"
worker = "python -m app.workers.order_consumer"
launch = "task dev && task worker"